# Project 2B Create NFT And used Dotevn module to load secret and infura key

1) Create file named .env
2) Add account secret and infura key in it, then in truffle-config.js use process.env.{entrykey}
